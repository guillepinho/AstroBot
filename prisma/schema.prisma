generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../AstroDB.db"
}

model usuarios {
  id      Int     @id @unique(map: "sqlite_autoindex_usuarios_1") @default(autoincrement())
  user    String
  dataNascimento    String?
}
