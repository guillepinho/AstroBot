const { EmbedBuilder } = require('discord.js');
const color = require('./color');

const astroEmbed = new EmbedBuilder()
  .setTitle('🎶 DJ Astro 🎶')
  .setColor(color);

module.exports = astroEmbed;
